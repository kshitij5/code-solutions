#include <bits/stdc++.h>
using namespace std;
typedef long long int lli;

enum {
    INDIAN,
    NON_INDIAN
};
int main(){
    lli t;  //no of test cases
    cin >> t;

    while (t--){
        int activity;
        string origin ="";
        cin >> activity >> origin;

        while ( activity-- ){
            string activities[activity];


            
        }

    }
    return 0;
}