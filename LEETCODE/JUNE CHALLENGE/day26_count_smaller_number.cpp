#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    //brute force
    // vector<int> countSmaller(vector<int>& nums) {
        
    //     vector<int> counts;
    //     for (int i = 0; i < nums.size(); i++) {
    //         int lessCount = 0;
    //         for (int j = i+1; j < nums.size(); j++) {
    //             if(nums[i]>nums[j]) lessCount++;
    //         }

    //         counts.push_back(lessCount);
    //     }

    //     return counts;
    // }

    //using dp and optimizattion
    vector<int> countSmaller(vector<int>& nums) {
        
    }
};